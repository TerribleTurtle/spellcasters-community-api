{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "$id": "https://spellcasters.gg/schemas/v2/balance_index.schema.json",
  "title": "Balance Index",
  "description": "Lookup table mapping entity IDs to their balance change type for the most recent patch. Used by deckbuilders for buff/nerf icons.",
  "type": "object",
  "required": ["patch_version", "patch_date", "entities"],
  "additionalProperties": false,
  "properties": {
    "patch_version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Semantic version of the patch this index represents."
    },
    "patch_date": {
      "type": "string",
      "description": "ISO 8601 date of the patch, or empty string if no patch has been published."
    },
    "entities": {
      "type": "object",
      "description": "Map of entity_id to balance change type.",
      "additionalProperties": {
        "$ref": "definitions/enums.schema.json#/definitions/patch_type_enum"
      }
    }
  }
}
